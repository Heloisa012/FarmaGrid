package com.example.projetoIntegrador.repository;

import com.example.projetoIntegrador.model.FarmaPaciente;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FarmaPacienteRepository extends JpaRepository<FarmaPaciente, Integer> {
}
